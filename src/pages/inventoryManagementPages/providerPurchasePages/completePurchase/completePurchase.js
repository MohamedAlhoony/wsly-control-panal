import React from 'react'
import {
    Menu,
    Input,
    Button,
    Icon,
    Form,
    MenuItem,
    Select,
} from 'semantic-ui-react'

export const CompletePurchase = (props) => {
    return <h1>HI</h1>
}
